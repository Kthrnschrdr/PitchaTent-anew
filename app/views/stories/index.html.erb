<h1>Stories</h1>
<a href="/person/<%= @person.id %>/story/new">Add a new story!</a>
<%@stories.each do |s|  %>
	<a href="/person/<%= @person.id %>/story/<%= s.id %>"><%= s.story %>
	<p><%= s.park.name %></p>	
<%end%>